# AEGIS dashboard

This directory receives sanitized dashboard artifacts from the local AEGIS node.

Expected files:
- `index.html` — current local/exported dashboard snapshot
- `status.json` — compact machine-readable summary

The dashboard is observational. It must not expose credentials or provide unrestricted remote-control actions.
